# 개발 기록 — 특수 능력 시스템

> 대상: 「우리는 아직 『 』입니다 — 대국」 (nan-duel-m1)
> 기준 설계: `docs/분기후보_특수능력.md` (능력 3종·각 1회·3종 공용, 확정본)
> 원칙: 상태는 store 한 곳 / 모듈은 이벤트로 통신 / 수치·텍스트는 데이터 / 로직은 순수함수.
> 이 문서는 구현 진행을 단계별로 남긴다(무엇을·왜·어떻게 검증했는지). 제출용 "AI 활용 기술 문서"에도 재사용.

## 확정 규칙 (요약)

- 공유 산 유지. 능력은 점수가 아니라 "판을 흔드는 행동".
- 3종 공용(플레이어=AI 동일), 대국 시작 시 보유, **각 능력 국(round)당 1회**, 국 바뀌면 리셋.
- 예언서(beforeDraw·정보) / 각색(beforeDiscard·손패 장르 변경) / 복선(insteadOfDraw·버림패 회수).
- 횟수·peek 장수 등은 `config.abilities`로 분리 → 숫자만 고쳐 밸런싱.

## 핵심 설계 판단 (구현 전 코드 재점검에서 도출)

- **뽑기 전 입력 구간이 없다** → 기존 `main.advance()`는 내 턴에 자동으로 뽑고 decide로 넘어감.
  예언서·복선은 "뽑기 전"에 써야 하므로, **"조건부 정지"** 채택:
  내 턴이고 뽑기 전 쓸 능력(예언서/복선)이 남아 있을 때만 자동 뽑기를 멈추고 능력 바를 띄운다.
  능력이 없거나 다 썼으면 예전처럼 자동 뽑기 → 평범한 턴엔 군더더기 없음.
- **각색 ↔ 수읽기 정합성**: 각색은 손패 카드를 같은 단계의 다른 장르 카드(실존 id `${genre}-${stage}`)로
  치환 → `handEval`이 그대로 동작. 다만 "수읽기(남은 장수)" 표시가 한 장 어긋남 → 원본이 미룬 이슈, v1 유지.
- **각색의 카드 치환은 실존 id로** → 판정·렌더 재사용, 유령 카드 없음.

---

## 1단계 — 데이터 · 순수 로직 · 상태

**새 파일**
- `src/data/abilities.json` — 능력 3종 정의(id·이름·kind·timing·설명). 텍스트/성질만.
- `src/logic/abilities.js` — 순수함수:
  `initAbilityState(cfg)`(국 시작 잔여), `canUse`/`spend`(예산),
  `applyForesight(wall,n)`(다음 n장 미리보기, 산 불변),
  `applyAdapt(hand,i,genre,cardMap)`(장르 교체·단계 유지),
  `applyForeshadow(hand,discards,i)`(버림패 회수).

**수정**
- `src/data/config.json` — `abilities` 블록(enabled·uses·peek 등) 추가.
- `src/logic/duel.js` — `newRound`에 `abilityInit` 받아 국마다 잔여 초기화(→ 리셋 자동).
  능력 상태 전이 3종: `useForesight`(→{round,peek}), `useAdapt`, `useForeshadow`(→decide, 산 불변).
  기존 호출/테스트와 하위호환(3번째 인자 기본값).

**검증**
- `test/abilities.mjs` 신규 25종 통과. 순수함수 불변성(원본 미변경)·산 불변·사용 차감·페이즈 전이 확인.
- 회귀: 헤드리스 48/0, 스모크 23/0 유지.

## 2단계 — 엔진 배선 (조건부 뽑기 전 정지)

**수정**
- `src/main.js`:
  - `abilities.json` 로드, `abilityInit`(공용) 계산, `newRound`에 전달.
  - `playerHasPreDraw(round)`: 내 턴에 예언서 또는 (버림패 있는) 복선이 남았는지.
  - `advance()` draw 분기: 위 조건이면 **자동 뽑기 정지**(능력 바 렌더 후 입력 대기).
  - 능력 intent 7종 배선: `foresight`·`draw`·`foreshadow-start`·`foreshadow-commit`·
    `adapt-start`·`adapt-commit`·`ability-cancel`. 선택값은 기존 컨벤션대로 `ui`에 쓰고 paramless intent.
  - 국/매치 시작 시 능력 관련 `ui` 초기화(`ABILITY_UI_RESET`).

## 3단계 — 능력 UI

**새 파일**
- `src/render/abilityUI.js` — `#ability-bar`(능력 버튼·잔여 표시)와 `#ability-panel`(트레이) 렌더.
  예언서 미리보기(순번 1·2·3), 복선 버림패 선택, 각색 장르 선택(현재 장르 제외).

**수정**
- `index.html` — `#ability-bar`, `#ability-panel` 컨테이너 추가.
- `src/input/controls.js` — 능력 바/트레이 클릭을 intent로 위임(선택은 `ui`에 기록).
- `css/style.css` — 능력 바·트레이·장르 버튼 스타일(기존 팔레트 재사용).
- `src/main.js` — `renderAll`에 `renderAbilities` 추가.
- `test/smoke.mjs` — `abilityUI.js`를 임포트·DOM id 검사 목록에 포함.

**검증 (헤드리스 + 실제 브라우저)**
- 노드 테스트: 능력 25/0, 헤드리스 48/0, 스모크 24/0(DOM id 22개·intent 14종 모두 배선).
- 브라우저(Chromium/Playwright) 통합 12/0, 콘솔·페이지 에러 0건:
  · 뽑기 전 정지에 예언서+뽑기 표시, 손패 7장
  · 예언서 → 다음 3장 미리보기, 잔여 0
  · 뽑기 → 손패 8장(decide)
  · 카드 선택 → 각색 → 현재 장르 제외 4종 제시 → 장르 변경(무협·결 → SF·결), 단계 유지, 잔여 0
- 스크린샷으로 배치·연출 육안 확인.

## 현재 상태

- 플레이어는 예언서·각색·복선을 실제로 사용할 수 있음(각 국 1회).
- **AI는 아직 능력을 쓰지 않음**(4단계). 예언서는 정보형이라 결정적 AI엔 효과가 작아 v1에서 비중 낮음.
- 배포: 로직·UI 모두 반영됐으나, 커밋·푸시는 `NAN-업데이트-올리기.bat`으로 사용자가 진행.

## 남은 단계

- 4단계: AI 능력 사용 v1(복선=회수로 완성/개선, 각색=텐파이 개선; 예언서 보조). 자가대국 완주 확인.
- 5단계: 능력을 섞은 시뮬로 "판 흔들림"·무크래시 관찰, 밸런스 재점검, 배포.
- 후속: 개인화(3종 중 선택), 테마별 색, 각색↔수읽기 정합성 보정.

---

## 부록 — 손패 자동 정렬 (추가 요청, 2026-07-22)

> 유저 요청으로 능력과 별개로 추가. 결정 로그엔 없던 항목(이 세션에서 확정).

- **정한 것**: 내 손패를 **장르(무협→SF→판타지→로맨스→호러) → 단계(기·승·전·결)** 순으로
  **항상 자동 정렬**해서 표시. 대상은 내 손패만.
- **구현**: `render/handUI.js`의 `renderHand`만 수정. 표시용 정렬 뷰를 만들되
  각 카드의 **원래 배열 인덱스를 `dataset.index`에 보존** → 탭·버리기·각색은 전부
  원래 인덱스 기준이라 게임 로직·판정엔 영향 0. `sortValue = 장르순위*10 + 단계`,
  장르순위는 cards.json의 genres 배열 순서에서 파생(하드코딩 회피).
- **검증**: node 회귀(능력 25 / 헤드리스 48 / 스모크 24) 유지, 브라우저 13/0.
  뽑은 카드·각색으로 장르 바뀐 카드도 즉시 제자리 재정렬 확인, 콘솔 에러 0.

---

## 부록 2 — 자동 뽑기 + 뽑은 카드 자동 선택 (UX 수정, 2026-07-22)

> 유저 피드백: "뽑기 버튼을 따로 두지 말고, 내 차례에 자동으로 뽑히고 그 카드가
> 자동 선택돼 있으면 좋겠다"(마작식). 이에 맞춰 흐름과 능력 타이밍을 조정.

- **변경**: '뽑기 전 조건부 정지'와 뽑기 버튼 제거. 내 차례가 되면 `advance()`가
  바로 자동 뽑기 → decide로 가고, 방금 뽑은 카드를 `ui.selectedIndex`로 자동 선택
  (버리기 버튼 즉시 노출, 원터치 츠모기리 가능).
- **능력 타이밍 이동**: 예언서·복선을 '뽑기 전'에서 **뽑은 뒤(decide)**로 이동.
  능력 바(예언서·각색·복선)는 이제 decide에서만 표시.
- **복선 재정의(자동 뽑기 모델)**: "뽑기 대신"이 불가하므로 →
  **이번에 뽑은 카드를 무르고(산 맨 아래로 되돌림) 내 버림패 1장을 회수**.
  순효과는 동일: 손패 8장 유지 / 산 잔량 불변 / 내 버림패 -1.
- **수정 파일**: `duel.js`(능력 3종 decide화·복선 재정의), `main.js`(정지 제거·자동 선택),
  `abilityUI.js`(decide 바), `controls.js`(뽑기 버튼 제거), `abilities.json`(timing),
  `test/abilities.mjs`(복선 규칙 반영).
- **검증**: 능력 유닛 27/0, 헤드리스 48/0, 스모크 24/0, 브라우저 13/0
  (뽑기 버튼 없음·자동 선택·정렬·예언서·각색 확인), 콘솔 에러 0.

---

## 부록 3 — 레이아웃 개편: 테이블 배치 (와이드/PC, 2026-07-22)

> 유저 요청 + 목업 확정("일단 이렇게 해보자"). 게임 로직은 안 건드리고 구조·CSS만.

- **테이블 배치**: 상대는 위 끝, 내 패는 아래 끝, 가운데 산은 넓게 비움.
  화면을 꽉 채우지 않고 여백을 둔 판(중앙 정렬). 데스크톱 우선.
- **왼쪽 고정 패널 "맞출 수 있는 패"** — 텐파이가 아니어도 항상 표시.
  · "지금 성립 중" = 손패에 성립한 세트(겹치지 않게 greedy).
  · 상황 힌트(완성/텐파이/버리면 텐파이) — 튜토리얼 코치 호환 위해 `.assist-*` 클래스 유지.
  · "노려볼 역" = 손패 구성 기반 가벼운 휴리스틱(방향 안내, 정확한 완성 판정 아님).
- **새 파일**: `src/logic/analysis.js`(순수: `formedSets`·`reachableYaku`) + `test/analysis.mjs`.
- **수정**: `index.html`(테이블 구조 — 모든 DOM id·재사용 클래스 보존),
  `css/style.css`(body 중앙정렬 + 테이블 레이아웃 블록), `render/handUI.js`(왼쪽 패널 렌더),
  `main.js`(dataBundle에 yakuData), `test/smoke.mjs`(analysis 임포트).
- **검증**: 분석 6/0 · 능력 27/0 · 헤드리스 48/0 · 스모크 25/0, 브라우저 기능 13/0,
  실제 게임 전체 스크린샷으로 배치 육안 확인, 콘솔 에러 0.
- **미정/후속**: "노려볼 역" 휴리스틱 정교화, 좁은 화면(모바일) 반응형.

---

## 부록 4 — 완성 컷인 · 역 예시 툴팁 · 코치마크 중앙 (2026-07-22)

> 유저 요청 3건. 게임 로직 불변, 렌더·연출·데이터만.

- **완성 컷인 (a+b 혼합)**: 모든 완성에 컷인 연출(완성 선언/운명 뺏기 → 헤드라인 → 점수),
  역만(불후의 명작·13점↑)·대작(6점↑)은 더 화려하게(금빛 광선·글로우). 탭하면 즉시 결과로(스킵·D5).
  `cutin.js`의 showRoundEnd를 **컷인 → 결과 모달** 2단계로 분리(`playWinCutin`+`renderRoundResult`).
- **역 예시 툴팁**: 왼쪽 "노려볼 역" 항목에 **hover(데스크톱)·탭(모바일)** 시 그 역의 예시 패를 옆에 표시.
  `yaku.json`에 `example`(예시 카드) 추가, `index.html`에 `#yaku-tip`, `handUI.js`에 툴팁 렌더.
- **코치마크 중앙 이동**: 손패가 하단에 붙어 가려지던 문제 → 코치마크를 **화면 중앙**에 띄우고
  대상은 하이라이트만 유지. `tutorial.js` showCoach + CSS `.coach-mark.centered`.
- **검증**: node(분석 6 / 능력 27 / 헤드리스 48 / 스모크 25) 전부 통과, 브라우저 기능 13/0,
  컷인(역만/일반)·툴팁·코치마크 스크린샷 육안 확인, 콘솔 에러 0.

---

## 부록 5 — 현행 게임 마무리: AI 능력·역만 버그·검증 (2026-07-22)

- **AI 능력 사용 (4단계)**: `ai.js aiChooseAbility` — v1 방침 "**이번 턴에 즉시 완성되면**"
  각색/복선 사용(이미 완성 가능하면 낭비 안 함). 예언서는 정보형이라 미사용.
  `main.js`에서 AI decide에 배선 + "라이벌: 각색/복선" 잠깐 표시(`#ai-flash`).
  `config.abilities.aiUse`(기본 true)로 on/off 가능한 밸런스 레버.
- **역만 선언 버그 수정**: `yakuEval` — 역만(불후의 명작)은 역 1개 취급이라 "최소 2역" 조건에
  걸려 선언 불가였음 → **역만은 최소역 조건 예외**로 수정.
- **도움말 정정**: 세트 = "같은 장르 3장(연속이면 정통 연재)"로 실제 규칙에 맞춤.
- **★ 밸런스 발견(sim_ability, 500국)**: AI 능력 켜면 **각색이 거의 매판 사용**되고
  국 길이 중앙값이 **8 → 4턴**으로 절반. "장르 한 장 바꿔 완성"이 강해서 AI가 빨리 이김.
  → `aiUse` 레버로 조절 가능. 밸런스 방향은 유저 결정 대기(끄기 / 완화 / 유지).
- **검증**: 능력 27 / 분석 6 / 헤드리스 48 / 스모크 25 통과, `sim_ability` 500국 무크래시 완주,
  브라우저 기능 13/0·콘솔 에러 0.

---

## 부록 6 — "국이 너무 빨리 끝난다" 해결: 완성 문턱 3역 + AI 각색 잠금 (2026-07-23)

> 유저 보고: "AI와 승부 시 3~5턴 내로 완성돼 버린다." 원인을 시뮬로 분해하고 값으로 조정.

**진단 (배포 엔진, 능력 OFF, 총 뽑기 = 양쪽 합)**
- `sim_structure`(코드 독립): 손패 8→11→14로 **늘려도 국 길이 거의 불변**(중앙값 13→14→15).
  → **패 수는 길이 레버가 아니다.** (게다가 handEval/AI는 8장 하드코딩 — 확장은 큰 재작성.)
- `sim_length`(배포 엔진): 실제 길이를 늘리는 값은 둘뿐 —
  · **론(운명 뺏기) OFF → 중앙값 8→13** (1인 6.5턴). 하지만 '운명 뺏기' 테마가 사라짐.
  · **최소 3역 → 중앙값 8→11** (1인 5.5턴). 테마 유지, 유국 0%. ← 채택.
- `sim_ability`: 능력 켜면 **각색이 즉시완성 버튼**이라 중앙값 **8→4**. 각색 469/500판 사용.

**조정 (값·플래그만, 로직 구조 불변)**
- `config.rules.minYakuToDeclare` **2 → 3**: 완성 문턱↑. 1인 4턴 → 5.5턴.
- `config.abilities.aiUse` **true → ["foresight","foreshadow"]**: AI는 **각색 미사용**.
  `aiChooseAbility(round, deps, allow)`에 허용 Set 인자 추가(각색·복선 게이트).
  `main.js`는 aiUse가 배열이면 그 집합만 허용(true=전부/false=없음/배열=목록).
- 도움말·가이드 문구 "2역 → 3역"으로 정정. index.html `?v=3` 캐시버스트.

**결과 (sim_ability 500국, 최종안)**
- 국 길이 중앙값 **4 → 10** (총 뽑기, ≈ 1인 2턴 → 5턴). 각색 0 / 복선 52 / 유국 0.
- 즉, 3~5턴 즉사 → **평균 5턴 안팎에서 완성**, 라이벌이 각색으로 순삭하지 않음.

**남은 레버(더 길게 원하면)**: 론을 '큰 손만' 또는 OFF(테마 손실), 손패 확장(재작성 필요).
플레이 감각 확인 후 방향 결정.

- **검증**: 능력 27 / 분석 6 / 헤드리스 48 / 스모크 25 통과, `sim_ability` 500국 무크래시,
  브라우저 로드 육안(손패 8·콘솔 에러 0).

---

## 부록 7 — 런 기반 완성 구조 전환 (2026-07-23)

> "너무 빨리 끝남 + 너무 쉬움"을 구조로 해결. 상세: `설계_런기반_완성구조_가산역.md`.

- **세트 = 같은 장르 연속(기승전/승전결)만.** 삼중첩·혼합 런 불인정(중복은 짝에만).
  `handEval.classifySet` 재작성 + `rules.allowGenreTriplet`(앤솔로지 안전판, 기본 off).
- **선언 게이트 = 승전결 세트 최소 1개** (기존 minYaku=3 폐기). `yakuEval`에서 `hasFinale`로 판정.
- **채점 축+등급**: 서사 완성도(양대완결2 / 대서사시3 / 일대기6) + 전집(+5) + 인연(+2) + 역만(13).
  같은 '대서사시'라도 **다른 장르 3 → 같은 장르(같은 작품) 6** 격 차등. yaku.json v2.
- **검증**: demo_run(예시 손패 표 일치), sim_run 400국(국 길이 ≈1인 10턴 / 유국 5~6% / 역 분포 양호),
  smoke25·headless46·abilities27·analysis6 통과, 브라우저 콘솔 에러 0.
- 다음 실험: HP 배틀러(패턴→데미지) — `분기후보_HP배틀러_패턴데미지.md`.
